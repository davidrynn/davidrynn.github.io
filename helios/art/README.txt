Drop images here (png, jpg, gif, webp, avif). They appear on /helios/ automatically, sorted by filename;
the filename becomes the caption (dashes and underscores turn into spaces). Keep each file under ~10 MB.
